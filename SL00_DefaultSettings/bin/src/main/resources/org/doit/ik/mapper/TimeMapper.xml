<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.doit.ik.mapper.TimeMapper">

  <select id="getTime" resultType="String">
    SELECT sysdate
    FROM dual
  </select>
  
  <!-- 
  <select id="getNextTime" resultType="String">
    SELECT sysdate + 1
    FROM dual
  </select>
  -->
  
</mapper>  